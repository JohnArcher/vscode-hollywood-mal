[
  {
    "name": "Beep",
    "helpId": "Beep",
    "shortDescription": "play the system beep",
    "version": "8.0",
    "synopsis": "Beep([type])",
    "functionDocs": "This function can be used to play the system's default beep sound. For systems which support different beep sounds for different contexts, you can pass the optional `type` argument to specify which beep sound should be played.\n\nThe following beep types are currently supported:\n\n- `#BEEPSYSTEM`: System beep sound. This is the default.\n\n- `#BEEPERROR`: Error beep sound.\n\n- `#BEEPWARNING`: Warning beep sound.\n\n- `#BEEPQUESTION`: Question beep sound.\n\n- `#BEEPINFORMATION`: Information beep sound.\n\nNote that not all operating systems support all kinds of beeps. Only `#BEEPSYSTEM` is supported on every platform.",
    "inputsDocs": "* `type` — optional: type of beep sound to play (defaults to `#BEEPSYSTEM`)",
    "example": "```\nBeep()\n```\n\nThe code above plays the default beep.",
    "platforms": ["All"],
    "insertText": "Beep()"
  },
  {
    "name": "CollectGarbage",
    "helpId": "CollectGarbage",
    "shortDescription": "force a garbage collection or set new threshold",
    "version": "2.0",
    "synopsis": "CollectGarbage([threshold])",
    "functionDocs": "This function can be used to force a garbage collection. To do that, you have to call `CollectGarbage()` without specifying the optional argument `threshold`. If you pass the optional threshold argument, however, then this value will be set as the new garbage collector threshold. This means that whenever the garbage size is bigger than the specified threshold in kilobytes, Hollywood will automatically run the garbage collector.\n\nIf you leave out the optional argument, Hollywood will immediately run the garbage collector and set a new threshold that is twice the size of garbage just collected.\n\nTo get information about the state of the garbage collector, call the [GCInfo()](https://www.hollywood-mal.com/docs/html/hollywood/GCInfo_.html) function.",
    "inputsDocs": "* `threshold` — optional: threshold in kilobytes that specifies when Hollywood should run the garbage collector (defaults to 0 which means run the garbage collector immediately)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "CollectGarbage()"
  },
  {
    "name": "DisableLineHook",
    "helpId": "DisableLineHook",
    "shortDescription": "disable the line hook",
    "version": "6.0",
    "synopsis": "DisableLineHook()",
    "functionDocs": "This function can be used to disable Hollywood's line hook. The line hook is a function which is automatically called by Hollywood after every code line. It is responsible for controlling several tasks that Hollywood needs for its housekeeping, for example event handling and video playback. Thus, it is very important that the line hook gets called several times per second at least, otherwise your application will become unresponsive and video playback will stutter. Disabling the line hook for a brief period of time, however, can increase the raw performance of Hollywood's virtual machine significantly since all the code in the linehook isn't executed at all. Hence, you may want to disable it temporarily if you need to do some complex calculations as fast as possible. But make sure to enable the line hook again as soon as possible to prevent your application from becoming unresponsive.\n\nTo enable the line hook again, you will have to call [EnableLineHook()](https://www.hollywood-mal.com/docs/html/hollywood/EnableLineHook_.html).",
    "inputsDocs": "none",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "DisableLineHook()"
  },
  {
    "name": "EnableLineHook",
    "helpId": "EnableLineHook",
    "shortDescription": "enable the line hook",
    "version": "6.0",
    "synopsis": "EnableLineHook()",
    "functionDocs": "This function can be used to enable Hollywood's line hook. The line hook is a function which is automatically called by Hollywood after every code line. It is responsible for controlling several tasks that Hollywood needs for its housekeeping, for example event handling and video playback. Thus, it is very important that the line hook gets called several times per second at least, otherwise your application will become unresponsive and video playback will stutter. Disabling the line hook for a brief period of time, however, can increase the raw performance of Hollywood's virtual machine significantly since all the code in the linehook isn't executed at all. Hence, you may want to disable it temporarily if you need to do some complex calculations as fast as possible. But make sure to enable the line hook again as soon as possible to prevent your application from becoming unresponsive.\n\nTo disable the line hook, you will have to call [DisableLineHook()](https://www.hollywood-mal.com/docs/html/hollywood/DisableLineHook_.html).",
    "inputsDocs": "none",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "EnableLineHook()"
  },
  {
    "name": "GCInfo",
    "helpId": "GCInfo",
    "shortDescription": "query garbage collector status",
    "version": "2.0",
    "synopsis": "count, threshold = GCInfo()",
    "functionDocs": "This function returns information about the current status of the garbage collector. The first return value tells you how many kilobytes of memory is currently occupied by Hollywood's VM whereas the second return value indicates the threshold in kilobytes that should trigger the garbage collector. Whenever memory consumption exceeds the specified threshold, Hollywood will automatically run the garbage collector.\n\nYou can also run the garbage collector manually or change the garbage collector threshold by calling the [CollectGarbage()](https://www.hollywood-mal.com/docs/html/hollywood/CollectGarbage_.html) function.",
    "inputsDocs": "none",
    "resultsDocs": "* `count` — amount of memory in kilobytes currently used by the Hollywood VM\n* `threshold` — current garbage collector threshold",
    "platforms": ["All"],
    "insertText": "GCInfo()"
  },
  {
    "name": "GetConstant",
    "helpId": "GetConstant",
    "shortDescription": "get constant's value from string",
    "version": "4.5",
    "synopsis": "val = GetConstant(c$)",
    "functionDocs": "This function returns the value of a constant that is passed in as a string. It is probably of not much use.",
    "inputsDocs": "* `c$` — constant inside string whose value is to be retrieved",
    "resultsDocs": "* `val` — value of the constant",
    "example": "```\nDebugPrint(#HOLLYWOOD, \"=\", GetConstant(\"#HOLLYWOOD\"))\n```\n\nObtains the constant `#HOLLYWOOD` first via direct specification and then via `GetConstant()`. The result should be the same.",
    "platforms": ["All"],
    "insertText": "GetConstant(${1:c$})"
  },
  {
    "name": "GetDefaultAdapter",
    "helpId": "GetDefaultAdapter",
    "shortDescription": "get default adapter for type",
    "version": "10.0",
    "synopsis": "adapter$ = GetDefaultAdapter(type)",
    "functionDocs": "This function gets the default adapter for the type specified by `type` and returns it. The adapter types that can be passed in `type` are described in the manual for the [SetDefaultAdapter()](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultAdapter_.html) function. See [SetDefaultAdapter](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultAdapter_.html).\n\nNote that if no default adapter has been set using [SetDefaultAdapter()](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultAdapter_.html) for a specific type, an empty string will be returned.",
    "inputsDocs": "* `type` — adapter type to query",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "GetDefaultAdapter(${1:type})"
  },
  {
    "name": "GetDefaultEncoding",
    "helpId": "GetDefaultEncoding",
    "shortDescription": "get default character encoding",
    "version": "7.0",
    "synopsis": "tencoding, sencoding = GetDefaultEncoding()",
    "functionDocs": "This function returns the default character encodings for the text library in `tencoding` and for the string library in `sencoding`. See [SetDefaultEncoding](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultEncoding_.html).",
    "inputsDocs": "none",
    "resultsDocs": "* `tencoding` — default character encoding for the text library\n* `sencoding` — default character encoding for the string library",
    "platforms": ["All"],
    "insertText": "GetDefaultEncoding()"
  },
  {
    "name": "GetDefaultLoader",
    "helpId": "GetDefaultLoader",
    "shortDescription": "get default loader for type",
    "version": "10.0",
    "synopsis": "loader$ = GetDefaultLoader(type)",
    "functionDocs": "This function gets the default loader for the type specified by `type` and returns it. The loader types that can be passed in `type` are described in the manual for the [SetDefaultLoader()](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultLoader_.html) function. See [SetDefaultLoader](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultLoader_.html).\n\nNote that if no default loader has been set using [SetDefaultLoader()](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultLoader_.html) for a specific type, an empty string will be returned.",
    "inputsDocs": "* `type` — loader type to query",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "GetDefaultLoader(${1:type})"
  },
  {
    "name": "GetMemoryInfo",
    "helpId": "GetMemoryInfo",
    "shortDescription": "get memory information",
    "version": "1.0",
    "synopsis": "space = GetMemoryInfo(type)",
    "functionDocs": "This function returns information about the amount of memory in your system. The following constants can be specified as `type`:\n\n- `#CHIPMEMORY`: Returns the amount of chip memory\n\n- `#FASTMEMORY`: Returns the amount of fast memory",
    "inputsDocs": "* `type` — one of the constants as listed above",
    "resultsDocs": "* `space` — memory space",
    "example": "```\nchip=GetMemoryInfo(#CHIPMEMORY)\nfast=GetMemoryInfo(#FASTMEMORY)\nPrint(\"You have\", chip, \"bytes of chip memory and\", fast,\n      \"bytes of fast memory!\")\n```\n\nThe above code prints out the chip and fast memory.",
    "platforms": ["All"],
    "insertText": "GetMemoryInfo(${1:type})"
  },
  {
    "name": "GetSystemInfo",
    "helpId": "GetSystemInfo",
    "shortDescription": "get OS-specific information",
    "version": "4.5",
    "synopsis": "t = GetSystemInfo()",
    "functionDocs": "This function can be used to query certain information from the operating system that Hollywood is currently running on. `GetSystemInfo()` returns a table that contains several fields which are different depending on the operating system Hollywood is currently running on.\n\nThe following fields will be initialized in the return table:\n\n- `UserHome`: Path to the user's home directory. Supported on Windows, macOS, and Linux. (V5.3)\n\n- `UserName`: Name of the current user. Supported on Windows, macOS, and Linux. (V5.3)\n\n- `ProgramFiles`: Path to the program files directory on this computer. Supported on Windows since V4.5 and on macOS since V5.3.\n\n- `AppData`: Path to the application data folder for the current user on this computer. Supported on Windows since V4.5, on macOS since V5.3, and on iOS since V7.0.\n\n- `CommonAppData`: Path to the application data folder for all users on this computer. Supported on Windows and macOS. (V6.1)\n\n- `LocalAppData`: Path to the local, non-roaming application data folder for the current user on this computer. Supported on Windows and macOS. On macOS, this will be set to the same path as `CommonAppData`. (V9.0)\n\n- `MyDocuments`: Path to the \"My documents\" folder on this computer. Supported on Windows since V4.5, on macOS since V5.3, and on iOS since V7.0.\n\n- `Windows`: Path to the Windows directory on this computer. Supported only on Windows. (V4.5)\n\n- `SDCard`: Path to the external storage device. This is called `SDCard` for legacy reasons because in the early days of Android, the external storage device typically was an SD card. This tag is only supported on Android. Also note that starting from Android 6.0, apps are no longer allowed to read from and write to this folder without explicit user permission. When using the Hollywood Player, it will automatically request such permission from the user on startup. When compiling stand-alone APKs using the Hollywood APK Compiler, though, you have to manually request read and/or write permission for this folder by using the [PermissionRequest()](https://www.hollywood-mal.com/docs/html/hollywood/PermissionRequest_.html) function. See [PermissionRequest](https://www.hollywood-mal.com/docs/html/hollywood/PermissionRequest_.html). (V5.1)\n\n- `ExternalStorage`: Path to the external storage folder on this device. Supported only on Android. (V5.1)\n\n- `InternalStorage`: Path to the internal storage folder on this device. Supported only on Android. (V5.1)\n\n- `AppBundle`: Path to the application bundle of the current program. Supported only on macOS (V6.1) and iOS (V7.0).\n\n- `Preferences`: The path that Hollywood uses to store preferences managed using [SavePrefs()](https://www.hollywood-mal.com/docs/html/hollywood/SavePrefs_.html). (V7.1)\n\n- `TempFiles`: A path that you can write temporary files to. (V7.1)",
    "inputsDocs": "none",
    "resultsDocs": "* `t` — a table containing the fields described above",
    "platforms": ["All"],
    "insertText": "GetSystemInfo()"
  },
  {
    "name": "GetType",
    "helpId": "GetType",
    "shortDescription": "examine a variable",
    "version": "2.0",
    "synopsis": "type = GetType(var)",
    "functionDocs": "This function can be used to find out the type of a variable or value. Possible return values are `#NUMBER`, `#STRING`, `#TABLE`, `#FUNCTION`, `#USERDATA`, `#LIGHTUSERDATA`, `#THREAD` and `#NIL`.\n\nThis function is often used to find out if a variable is [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html). Starting with Hollywood 6.0, however, there is also a new convenience function named [IsNil()](https://www.hollywood-mal.com/docs/html/hollywood/IsNil_.html) which can also be used to check variables against [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html).\n\nSee [Data types](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeIntro_.html).",
    "inputsDocs": "* `var` — variable to examine",
    "example": "```\ntype = GetType(\"Hello World\")\n```\n\nThis will return `#STRING`.\n\n---\n\n```\ntype = GetType({1, 2, 3, 4})\n```\n\nThis will return `#TABLE`.\n\n---\n\n```\ntype = GetType(Function() DebugPrint(\"Hello\") EndFunction)\n```\n\nThis will return `#FUNCTION`.",
    "platforms": ["All"],
    "insertText": "GetType(${1:var})"
  },
  {
    "name": "GetVersion",
    "helpId": "GetVersion",
    "shortDescription": "get information about the Hollywood version in use",
    "version": "3.0",
    "synopsis": "t = GetVersion()",
    "functionDocs": "This function can be used to obtain some information about the Hollywood version currently running your script or applet. You can also retrieve information about the platform that your script is running on.\n\nThis function will return a table with the following fields initialized:\n\n- `Application`: This will be \"Hollywood\", \"Miniwood\", \"HollywoodPlayer\" or \"MiniwoodPlayer\" depending on which version is in use. If you compile your script into an executable, this will always be \"HollywoodPlayer\" or \"MiniwoodPlayer\" if you use Miniwood.\n\n- `Version`: A string containing the version of Hollywood, e.g. \"3.0\".\n\n- `Version_Date`: A string containing the build date of this Hollywood version.\n\n- `Kernel`: A string containing the kernel version of Hollywood, e.g. \"3.0\".\n\n- `Kernel_Date`: A string containing the build date of the Hollywood kernel.\n\n- `Beta`: This field will be set to `True` if a beta version of Hollywood is used, `False` otherwise.\n\n- `Platform`: This is probably the most useful field because it contains the platform on which Hollywood is currently running. This field can be \"AmigaOS3\", \"MorphOS\", \"WarpOS\", \"AmigaOS4\", \"AROS\", \"Win32\", \"MacOS\", \"Linux\", \"iOS\", or \"Android\". Note that \"Win32\" is also returned for 64-bit Windows. It's called \"Win32\" for historical reasons.\n\n- `Demo`: This field will be set to `True` if the user is running a demo version of Hollywood. (V4.71)\n\n- `Plugins`: This field will be set to a table that contains information about all plugins that have been loaded by Hollywood for this script. See [GetPlugins](https://www.hollywood-mal.com/docs/html/hollywood/GetPlugins_.html). (V5.1)\n\n- `CPU`: This field will be set to the CPU architecture that Hollywood is currently running on. This field can be \"m68k\", \"m68k/ppc\" (for WarpOS), \"ppc\", \"arm\", \"arm64\", \"i386\", or \"x64\". (V5.2)\n\n- `BigEndian`: This field will be set to `True` if Hollywood is running on a big endian CPU (i.e. 68000 or PowerPC) or `False` for a little endian CPU (x86, x64 and ARM). (V6.0)",
    "inputsDocs": "none",
    "resultsDocs": "* `t` — a table containing information about the Hollywood version in use",
    "example": "```\nt = GetVersion()\nIf t.platform = \"Win32\" Then Error(\"Sorry, Win32 is not supported yet!\")\n```\n\nThe code above checks on which version we are running and exits with an error if Hollywood is running on Windows.",
    "platforms": ["All"],
    "insertText": "GetVersion()"
  },
  {
    "name": "IsNil",
    "helpId": "IsNil",
    "shortDescription": "check if a variable is Nil",
    "version": "6.0",
    "synopsis": "bool = IsNil(var)",
    "functionDocs": "This function checks if the specified variable is [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html). Along with [GetType()](https://www.hollywood-mal.com/docs/html/hollywood/GetType_.html) `IsNil()` is the only reliable way to find out if a variable is [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) or not. Checking the variable against the [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) identifier is not reliable because this will also result in `True` if the variable is zero instead of [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html). Example:\n\n```\na = 0\nb = Nil\nDebugPrint(IsNil(a), a = Nil)  ; prints \"0 1\"\nDebugPrint(IsNil(b), b = Nil)  ; prints \"1 1\"\n```\n\nYou see that \"a = Nil\" returns `True` although `a` is zero and not [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html). That is because [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) is always regarded as zero when used in expressions. Thus, if you want to find out whether a variable really is [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html), always use `IsNil()` or [GetType()](https://www.hollywood-mal.com/docs/html/hollywood/GetType_.html).\n\nSee [Data types](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeIntro_.html).",
    "inputsDocs": "* `var` — variable to examine",
    "resultsDocs": "* `bool` — `True` if the variable is `Nil`, `False` otherwise",
    "platforms": ["All"],
    "insertText": "IsNil(${1:var})"
  },
  {
    "name": "IsUnicode",
    "helpId": "IsUnicode",
    "shortDescription": "determine if Hollywood is in Unicode mode",
    "version": "7.0",
    "synopsis": "bool = IsUnicode()",
    "functionDocs": "This function returns `True` if Hollywood is currently in Unicode mode, `False` otherwise. Since scripts should always run in Unicode mode, this function is probably of not much use.",
    "inputsDocs": "none",
    "resultsDocs": "* `bool` — `True` if Hollywood is in Unicode mode, `False` otherwise",
    "platforms": ["All"],
    "insertText": "IsUnicode()"
  },
  {
    "name": "LegacyControl",
    "helpId": "LegacyControl",
    "shortDescription": "enable or disable certain legacy features",
    "version": "6.0",
    "synopsis": "LegacyControl(feature$, enable)",
    "functionDocs": "This function can be used to enable or disable certain legacy functionality that Hollywood still supports for compatibility reasons. You have to pass the name of the feature that you want to address as well as `True` to enable this feature or `False` to disable it.\n\nThe following strings can currently be passed in `feature$`:\n\n- `SingleMusic`: Before Hollywood 6.0 only one music could be playing at a time. This limitation has been removed with Hollywood 6.0 but by default, PlayMusic() will still stop any playing music in order to be fully compatible with previous versions. If you don't want that, you have to call `LegacyControl()` and set `SingleMusic` to `False`. Hollywood will then be able to play multiple music objects at the same time. This tag defaults to `True`.\n\n- `LineBasedShapes`: Before Hollywood 6.0 all round shapes drawn by the functions [Arc()](https://www.hollywood-mal.com/docs/html/hollywood/Arc_.html), [Circle()](https://www.hollywood-mal.com/docs/html/hollywood/Circle_.html), [Ellipse()](https://www.hollywood-mal.com/docs/html/hollywood/Ellipse_.html) and [Box()](https://www.hollywood-mal.com/docs/html/hollywood/Box_.html) (when using the optional parameter to draw a box with round corners) were drawn as polygons which made them look rather square. Starting with Hollywood 6.0 these round shapes are now drawn as real B�zier splines if antialiasing is turned on. This will be a little slower than the polyline-based approach from previous versions, but it will look much better. If you want Hollywood to keep using the polygon-based approach from previous versions, you can set this tag to `True`. In that case, the functions listed above will draw shapes that look exactly the same as the ones drawn by previous versions. This tag defaults to `False` except when running an applet compiled by Hollywood versions older than 6.0. In that case it defaults to `True`.\n\n- `LegacyRequesters`: Starting with Hollywood 11, [FileRequest()](https://www.hollywood-mal.com/docs/html/hollywood/FileRequest_.html) and [PathRequest()](https://www.hollywood-mal.com/docs/html/hollywood/PathRequest_.html) will use Android's document framework and return Storage Access Framework (SAF) URIs instead of traditional paths. If you don't want that, you can set this tag to `True` to get the old requesters back. Keep in mind, though, that the old requesters will only work if your app has been granted the manage external permission which can be queried via [PermissionRequest()](https://www.hollywood-mal.com/docs/html/hollywood/PermissionRequest_.html). See [PermissionRequest](https://www.hollywood-mal.com/docs/html/hollywood/PermissionRequest_.html).",
    "inputsDocs": "* `feature$` — name of the feature to enable or disable (see above)\n* `enable` — `True` to enable the feature, `False` to disable it",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "LegacyControl(${1:feature$}, ${2:enable})"
  },
  {
    "name": "OpenURL",
    "helpId": "OpenURL",
    "shortDescription": "open URL in default web browser",
    "version": "4.5",
    "synopsis": "OpenURL(url$)",
    "functionDocs": "This function opens the specified in the default web browser.",
    "inputsDocs": "* `url$` — URL to open",
    "example": "```\nOpenURL(\"http://www.airsoftsoftwair.com/\")\n```\n\nThe code above takes you directly to the Airsoft Softwair headquarters.",
    "platforms": ["All"],
    "insertText": "OpenURL(${1:url$})"
  },
  {
    "name": "SetDefaultAdapter",
    "helpId": "SetDefaultAdapter",
    "shortDescription": "set default adapter",
    "version": "10.0",
    "synopsis": "SetDefaultAdapter(type, adapter$)",
    "functionDocs": "This function can be used to set the default adapter for the type specified by `type` to the adapter specified by `adapter$`. All Hollywood functions that support adapters of the specified type will then default to the adapter set using this function.\n\nThe following adapter types are currently supported by Hollywood and can be passed in `type`:\n\n- `#ADAPTER_FILE`: File, directory, and filesystem adapters. They are used to open files, directories and handle filesystem operation. They can be passed to all functions that deal with files.\n\n- `#ADAPTER_NETWORK`: Network adapters. They can be used to replace Hollywood's inbuilt network handler. Functions that support network adapters include [OpenConnection()](https://www.hollywood-mal.com/docs/html/hollywood/OpenConnection_.html) and [DownloadFile()](https://www.hollywood-mal.com/docs/html/hollywood/DownloadFile_.html).\n\n- `#ADAPTER_SERIALIZE`: Serializer adapters. They are used to serialize and deserialize data. Functions that support serializer adapters are [SerializeTable()](https://www.hollywood-mal.com/docs/html/hollywood/SerializeTable_.html) and [DeserializeTable()](https://www.hollywood-mal.com/docs/html/hollywood/DeserializeTable_.html) among others.\n\nThe default adapter for all the adapter types listed above is `default` which means that plugin adapters will always be asked before Hollywood's inbuilt handlers.\n\nThe string you pass in `adapter$` can contain one or more adapters. If several adapters are specified, they must be separated by `|` characters. See [Loaders and adapters](https://www.hollywood-mal.com/docs/html/hollywood/LoaderAdapterInfo_.html).",
    "inputsDocs": "* `type` — adapter type to use (see above for possible values)\n* `adapter$` — new default for the specified adapter",
    "example": "```\nSetDefaultAdapter(#ADAPTER_FILE, \"inbuilt\")\nOpenFile(1, \"test.txt\")\n```\n\nThe code above will set the default file adapter to `inbuilt`. This means that all Hollywood functions that deal with files will no longer support any plugin file adapters because you've told Hollywood to only use the inbuilt file adapter.",
    "platforms": ["All"],
    "insertText": "SetDefaultAdapter(${1:type}, ${2:adapter$})"
  },
  {
    "name": "SetDefaultEncoding",
    "helpId": "SetDefaultEncoding",
    "shortDescription": "set default character encoding",
    "version": "4.7",
    "synopsis": "SetDefaultEncoding(tencoding[, sencoding])",
    "functionDocs": "This function can be used to change the default character encoding for the text and string libraries. Note that for reasons of compatibility Hollywood maintains two different default character encodings: one for the text library and one for the string library. Under normal conditions, however, both default encodings should be set the to the same character encoding.\n\nThe default character encoding for the text library is specified in `tencoding` and affects functions such as [Print()](https://www.hollywood-mal.com/docs/html/hollywood/Print_.html), [NPrint()](https://www.hollywood-mal.com/docs/html/hollywood/NPrint_.html), [TextOut()](https://www.hollywood-mal.com/docs/html/hollywood/TextOut_.html), and [CreateTextObject()](https://www.hollywood-mal.com/docs/html/hollywood/CreateTextObject_.html).\n\nThe default character encoding for the string library needs to be specified in the `sencoding` parameter and affects most functions of the string library, i.e. functions such as [ReplaceStr()](https://www.hollywood-mal.com/docs/html/hollywood/ReplaceStr_.html) and [StrLen()](https://www.hollywood-mal.com/docs/html/hollywood/StrLen_.html).\n\nThe following character encodings are currently supported by Hollywood:\n\n- `#ENCODING_UTF8`: Use UTF-8 encoding. This is the default since Hollywood 7.0.\n\n- `#ENCODING_ISO8859_1`: This was the default encoding before Hollywood 7.0. This can be useful in case you need to deal with binary data or strings that aren't formatted as UTF-8. Don't be confused by the name: Even though the constant is called ISO 8859-1 it can actually be used with all kinds of non-UTF-8 encodings because for most string library functions it won't make a difference if the encoding is ISO 8859-1 or some other charset as long as one character is one byte which is true for all non-UTF-8 8-bit encodings. The only commands that won't work with non-ISO-8859-1 encodings are commands like [UpperStr()](https://www.hollywood-mal.com/docs/html/hollywood/UpperStr_.html), [LowerStr()](https://www.hollywood-mal.com/docs/html/hollywood/LowerStr_.html), etc. because they will do all upper and lower case mapping based on the ISO 8859-1 charmap and other encodings will require different charmaps so those functions won't give correct results for non-ISO-8859-1 text. Since `#ENCODING_ISO8859_1` can also be used with other encodings there's also the synonym constant `#ENCODING_RAW` which might be less misleading semantically because it doesn't suggest that strings are in ISO 8859-1 format (see below).\n\n- `#ENCODING_RAW`: This is the same as `#ENCODING_ISO8859_1` but using this instead of `#ENCODING_ISO8859_1` might be preferable from a semantic point of view because it doesn't suggest that strings are or must be in ISO 8859-1 encoding. Instead it simply says that strings are simply treated as a sequence of raw 8-bit characters.",
    "inputsDocs": "* `tencoding` — default character encoding for the text library\n* `sencoding` — default character encoding for the string library (V7.0)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetDefaultEncoding(${1:tencoding})"
  },
  {
    "name": "SetDefaultLoader",
    "helpId": "SetDefaultLoader",
    "shortDescription": "set default loader",
    "version": "10.0",
    "synopsis": "SetDefaultLoader(type, loader$)",
    "functionDocs": "This function can be used to set the default loader for the type specified by `type` to the loader specified by `loader$`. All Hollywood functions that support loaders of the specified type will then default to the loader set using this function.\n\nThe following loader types are currently supported by Hollywood and can be passed in `type`:\n\n- `#LOADER_IMAGE`: Image loaders, used for example by functions like [LoadBrush()](https://www.hollywood-mal.com/docs/html/hollywood/LoadBrush_.html).\n\n- `#LOADER_ANIM`: Animation loaders, used for example by functions like [OpenAnim()](https://www.hollywood-mal.com/docs/html/hollywood/OpenAnim_.html).\n\n- `#LOADER_SOUND`: Sound loaders, used for example by functions like [OpenMusic()](https://www.hollywood-mal.com/docs/html/hollywood/OpenMusic_.html).\n\n- `#LOADER_VIDEO`: Video loaders, used for example by functions like [OpenVideo()](https://www.hollywood-mal.com/docs/html/hollywood/OpenVideo_.html).\n\n- `#LOADER_ICON`: Icon loaders, used for example by functions like [LoadIcon()](https://www.hollywood-mal.com/docs/html/hollywood/LoadIcon_.html).\n\n- `#LOADER_FONT`: Font loaders, used for example by functions like [OpenFont()](https://www.hollywood-mal.com/docs/html/hollywood/OpenFont_.html).\n\nThe default loader for all the loader types listed above is `default` which means that Hollywood will first ask plugin loaders, then inbuilt loaders, then native loaders to open a file. An exception is `#LOADER_FONT` where, due to compatibility reasons, only native loaders will be asked to open the font.\n\nThe string you pass in `loader$` can contain one or more loaders. If several loaders are specified, they must be separated by `|` characters. See [Loaders and adapters](https://www.hollywood-mal.com/docs/html/hollywood/LoaderAdapterInfo_.html).",
    "inputsDocs": "* `type` — loader type to use (see above for possible values)\n* `loader$` — new default for the specified loader",
    "example": "```\nSetDefaultLoader(#LOADER_IMAGE, \"plugin\")\nLoadBrush(1, \"test.jpg\")\n```\n\nThe code above will set the default image loader to `plugin`. This means that functions like [LoadBrush()](https://www.hollywood-mal.com/docs/html/hollywood/LoadBrush_.html) will no longer use Hollywood's inbuilt image loader or the image loader provided by the OS. Only plugin image loaders will be asked to load image files.",
    "platforms": ["All"],
    "insertText": "SetDefaultLoader(${1:type}, ${2:loader$})"
  },
  {
    "name": "SetVarType",
    "helpId": "SetVarType",
    "shortDescription": "declare a variable / OBSOLETE",
    "version": "1.0",
    "synopsis": "SetVarType(var, type[, arraysize])",
    "functionDocs": "As of Hollywood 2.0, this function is obsolete and only included for compatibility reasons.\n\nThis function can be used to declare a variable. You only have to declare variables that are different from the default type (`#LONG`) and which do not have an identifier in their name like the \"$\" for strings or the \"!\" for floats. Arrays always have to be declared by specifying the optional argument arraysize.",
    "inputsDocs": "* `var` — variable name to declare\n* `type` — desired type (can be `#LONG`, `#FLOAT`, `#STRING`)\n* `arraysize` — optional: if you specify this argument, an array of the specified size will be allocated for you instead of a single variable",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetVarType(${1:var}, ${2:type})"
  },
  {
    "name": "ShowDirectory",
    "helpId": "ShowDirectory",
    "shortDescription": "show directory on desktop",
    "version": "11.0",
    "synopsis": "ShowDirectory(d$[, t])",
    "functionDocs": "This function opens the directory specified by `d$` using the host platform's desktop system (e.g. Explorer on Windows, Finder on macOS, Workbench on Amiga...).\n\nAdditional options can be specified using the optional table argument `t`. The following tags are currently recognized:\n\n- `ShowAll`: This tag is only supported on AmigaOS and compatibles. If it is set to `False`, only files that have an icon will be shown, otherwise all files will be shown. Defaults to `True`.\n\n- `ViewMode`: This tag is only supported on AmigaOS and compatibles. It allows you to set the Workbench view mode for the directory. This can be one of the following special constants:\n\n  - `#VIEWMODE_ICONS` View files as icons. This is the default.\n\n  - `#VIEWMODE_LIST` View files as a list.\n\n  Defaults to `#VIEWMODE_ICONS`.",
    "inputsDocs": "* `d$` — directory to open\n* `t` — optional: table containing further options (see above)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "ShowDirectory(${1:d$})"
  },
  {
    "name": "ShowNotification",
    "helpId": "ShowNotification",
    "shortDescription": "show system notification",
    "version": "8.0",
    "synopsis": "ShowNotification(title$, msg$[, table])",
    "functionDocs": "This function can be used to pop up a system notification box that shows the message passed in `msg$` to the user. The notification box will use the title specified in `title$`. If you pass an empty string (\"\") in the `title$` parameter, the notification will use the title specified in the [@APPTITLE](https://www.hollywood-mal.com/docs/html/hollywood/atAPPTITLE_.html) preprocessor command.\n\nThe optional table argument can be used to configure further parameters for the system notification. The following tags are currently recognized by the `table` argument:\n\n- `Icon`: This tag can be used to set the icon that should be shown inside the notification box if the host system supports it. The following icon types can currently be specified here:\n\n  - `#REQICON_NONE`: No icon\n  - `#REQICON_INFORMATION`: An information sign\n  - `#REQICON_ERROR`: An error sign\n  - `#REQICON_WARNING`: A warning sign\n  - `#REQICON_QUESTION`: A question mark\n\n  Note that not all platforms support this tag.\n\n- `Timeout`: This tag can be used to specify a timeout in milliseconds for the notification. If this tag is set, the notification will disappear after the specified time (in milliseconds) has elapsed. Otherwise the notification will use the host system's default timeout value for notifications. You can also pass one of the following special constants here:\n\n  - `#DURATION_SHORT`: Use this for a short notification.\n\n  - `#DURATION_LONG`: Use this for a long notification.\n\n  Note that on Android, you must pass one of the special constants listed above. Passing a millisecond value directly isn't supported on Android. Also note that not all platforms support the `Timeout` tag.\n\n- `X`: Desired horizontal position for the notification. This can also be one of Hollywood's special coordinate constants like `#CENTER`. This tag is only supported on Android and iOS.\n\n- `Y`: Desired vertical position for the notification. This can also be one of Hollywood's special coordinate constants like `#CENTER`. This tag is only supported on Android and iOS.\n\n- `NoSound`: Set this tag to `True` if you don't want any sound to accompany the notification. This tag is currently only supported on macOS.\n\nNote that on Windows notifications can only be shown if your application has a tray icon. Thus, `ShowNotification()` will automatically add a tray icon for your application if there is none yet. If you would like to manually install a tray icon for your application on Windows, call [SetTrayIcon()](https://www.hollywood-mal.com/docs/html/hollywood/SetTrayIcon_.html) before calling `ShowNotification()`. See [SetTrayIcon](https://www.hollywood-mal.com/docs/html/hollywood/SetTrayIcon_.html).\n\nAlso note that on AmigaOS 4 `ShowNotification()` is implemented through application.library's Ringhio system and thus can only be used if your script has registered itself as an AmigaOS 4 application using the `RegisterApplication` tag in [@OPTIONS](https://www.hollywood-mal.com/docs/html/hollywood/atOPTIONS_.html). If you need fine-tuned control over Ringhio messages and you don't need to be portable, you can also use the [ShowRinghioMessage()](https://www.hollywood-mal.com/docs/html/hollywood/ShowRinghioMessage_.html) function on AmigaOS 4 instead because this offers even more configuration options. See [ShowRinghioMessage](https://www.hollywood-mal.com/docs/html/hollywood/ShowRinghioMessage_.html).\n\nOn MorphOS `ShowNotification()` uses MagicBeacon's notification system. On AmigaOS 3 Ranchero is used for showing notifications. Note that since apps need to register before they can show notifications with Ranchero it's recommended that if you intend to use `ShowNotification()` on AmigaOS 3 with Ranchero you should also use [@APPIDENTIFIER](https://www.hollywood-mal.com/docs/html/hollywood/atAPPIDENTIFIER_.html) to specify a unique ID for your app. Otherwise the generic app name \"Hollywood\" will be used.",
    "inputsDocs": "* `title$` — desired title for the notification or empty string for default title\n* `msg$` — desired message text for the notification\n* `table` — optional: table containing further configuration parameters (see above)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "ShowNotification(${1:title$}, ${2:msg$})"
  }
]
