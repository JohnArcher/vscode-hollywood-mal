[
  {
    "name": "AddArcToPath",
    "helpId": "AddArcToPath",
    "shortDescription": "add elliptical arc to path",
    "version": "5.0",
    "synopsis": "AddArcToPath(id, xc, yc, ra, rb, start, end[, clockwise])",
    "functionDocs": "This function adds an elliptical arc to the path specified in the first argument. You have to provide the center point of the arc in the `xc` and `yc` arguments. The arc's radii have to be passed in `ra` and `rb`, and the start and end angles have to be specified in the `start` and `end` arguments respectively. All angles must be specified in degrees. If you want to have a closed ellipse, the start argument needs to be 0 and the end argument needs to be 360. Using the [AddEllipseToPath()](https://www.hollywood-mal.com/docs/html/hollywood/AddEllipseToPath_.html) command is of course easier in this case. The optional argument `clockwise` can be used to specify whether or not the elliptical arc shall be drawn in clockwise direction. This tag defaults to `True` which means clockwise drawing. If you set it to `False`, `AddArcToPath()` will connect the angles in anti- clockwise direction.\n\nNote that `AddArcToPath()` doesn't add a center point vertex. If you want the start and end angles of the arc to be connected with the center point, you need to do this manually by calling [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) before `AddArcToPath()` and [LineTo()](https://www.hollywood-mal.com/docs/html/hollywood/LineTo_.html) afterwards.\n\nAlso note that `AddArcToPath()` only starts a new subpath in case there is no active subpath. Otherwise it will simply connect its vertices to the currently active subpath. If you don't want this, you'll have to manually open a new subpath before calling `AddArcToPath()`. Furthermore, `AddArcToPath()` also won't close the active subpath when it is finished.",
    "inputsDocs": "* `id` — identifier of path to which the elliptical arc should be added\n* `xc` — x center point of arc\n* `yc` — y center point of arc\n* `ra` — arc radius on the x axis\n* `rb` — arc radius on the y axis\n* `start` — start angle in degrees (must be positive)\n* `end` — end angle in degrees (must be positive)\n* `clockwise` — optional: whether or not the angles should be connected in clockwise direction (defaults to `True` which means clockwise)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "AddArcToPath(${1:id}, ${2:xc}, ${3:yc}, ${4:ra}, ${5:rb}, ${6:start}, ${7:end})"
  },
  {
    "name": "AddBoxToPath",
    "helpId": "AddBoxToPath",
    "shortDescription": "add rectangle to path",
    "version": "5.0",
    "synopsis": "AddBoxToPath(id, x, y, width, height[, table])",
    "functionDocs": "This function adds a rectangle to the path specified in `id`. You have to provide the position and size of the rectangle in the other arguments. `AddBoxToPath()` will start a new subpath for the rectangle and close it.\n\nThe optional argument `table` allows you to configure the rectangle's style. The following tags are currently recognized:\n\n- `RoundLevel`: You can specify this tag to create a rectangle with rounded corners. You need to pass a round level in percentage which specifies how round the corners will be (possible values are 0 to 100). Defaults to 0 which means no round corners.\n\n- `CornerA, CornerB, CornerC, CornerD`: These four tags allow you to fine-tune the corner rounding of the rectangle. You can specify a rounding level (0 to 100) for every corner of the rectangle thus allowing you to create a rectangle where not all corners are rounded, or where the different corners use different rounding levels. These tags will override any setting specified in the `RoundLevel` tag.",
    "inputsDocs": "* `id` — identifier of path to which the rectangle should be added\n* `x` — x position of the rectangle\n* `y` — y position of the rectangle\n* `width` — rectangle width\n* `height` — rectangle height\n* `table` — optional: table containing further options (see above)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "AddBoxToPath(${1:id}, ${2:x}, ${3:y}, ${4:width}, ${5:height})"
  },
  {
    "name": "AddCircleToPath",
    "helpId": "AddCircleToPath",
    "shortDescription": "add circle to path",
    "version": "5.0",
    "synopsis": "AddCircleToPath(id, xc, yc, radius)",
    "functionDocs": "This function adds a circle to the path specified in the first argument. You have to provide the center point of the circle in the `xc` and `yc` arguments. The circle's radius has to be passed in the fourth argument.\n\nNote that `AddCircleToPath()` only starts a new subpath in case there is no active subpath. Otherwise it will simply connect its vertices to the currently active subpath. If you don't want this, you'll have to manually open a new subpath before calling `AddCircleToPath()`. Furthermore, `AddCircleToPath()` also won't close the active subpath when it is finished.",
    "inputsDocs": "* `id` — identifier of path to which the circle should be added\n* `xc` — x center point of circle\n* `yc` — y center point of circle\n* `radius` — circle's radius",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "AddCircleToPath(${1:id}, ${2:xc}, ${3:yc}, ${4:radius})"
  },
  {
    "name": "AddEllipseToPath",
    "helpId": "AddEllipseToPath",
    "shortDescription": "add ellipse to path",
    "version": "5.0",
    "synopsis": "AddEllipseToPath(id, xc, yc, ra, rb)",
    "functionDocs": "This function adds an ellipse to the path specified in the first argument. You have to provide the center point of the ellipse in the `xc` and `yc` arguments. The ellipse's radii have to be passed in `ra` and `rb`.\n\nNote that `AddEllipseToPath()` only starts a new subpath in case there is no active subpath. Otherwise it will simply connect its vertices to the currently active subpath. If you don't want this, you'll have to manually open a new subpath before calling `AddEllipseToPath()`. Furthermore, `AddEllipseToPath()` also won't close the active subpath when it is finished.",
    "inputsDocs": "* `id` — identifier of path to which the ellipse should be added\n* `xc` — x center point of ellipse\n* `yc` — y center point of ellipse\n* `ra` — ellipse radius on the x axis\n* `rb` — ellipse radius on the y axis",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "AddEllipseToPath(${1:id}, ${2:xc}, ${3:yc}, ${4:ra}, ${5:rb})"
  },
  {
    "name": "AddTextToPath",
    "helpId": "AddTextToPath",
    "shortDescription": "add vector text to path",
    "version": "5.0",
    "synopsis": "AddTextToPath(id, t$[, table])",
    "functionDocs": "This function adds the text specified in `t$` to the current path. The text will be added to the path as vector graphics. Because of that, this command will only work when a vector font (e.g. a TrueType font) is currently active. It will not work with bitmap fonts. `AddTextToPath()` will use the active font that was set by the last call to [SetFont()](https://www.hollywood-mal.com/docs/html/hollywood/SetFont_.html) or [UseFont()](https://www.hollywood-mal.com/docs/html/hollywood/UseFont_.html).\n\nPlease note that `AddTextToPath()` will add the text above the current y-point. Thus, if the path's current y-point is 240 and you add text to it that is 36 pixels high, the text will be placed at an y-position of 204 (240-36=204) instead of 240 as the current y-point might suggest.\n\nPlease note that there are currently some restrictions:\n\n- The font to be used with `AddTextToPath()` must have been opened using `#FONTENGINE_INBUILT`. Fonts that have been opened using `#FONTENGINE_NATIVE` will currently not work. The default inbuilt fonts `#SANS`, `#SERIF`, and `#MONOSPACE` will work just fine with `AddTextToPath()`.\n\n- Any styles set by [SetFontStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetFontStyle_.html) will be ignored by `AddTextToPath()`. If you want italic or bold text, you need to open a separate font that has the bold and/or italicized vector graphics already in its face data. Simply calling [SetFontStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetFontStyle_.html) does not work with vector text.\n\n- Similarly, all formatting tags that are recognized by [Print()](https://www.hollywood-mal.com/docs/html/hollywood/Print_.html), [TextOut()](https://www.hollywood-mal.com/docs/html/hollywood/TextOut_.html), and [CreateTextObject()](https://www.hollywood-mal.com/docs/html/hollywood/CreateTextObject_.html) are ignored by `AddTextToPath()`. It is currently not possible to use formatting tags with this command.\n\n- Newline characters ('\\n') are also ignored by this function.\n\nThe optional argument `table` allows you to specify further options. The following tags are currently recognized:\n\n- `Encoding`: This argument can be used to specify the character encoding inside `t$`. This defaults to the text library default encoding set by [SetDefaultEncoding()](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultEncoding_.html)). See [SetDefaultEncoding](https://www.hollywood-mal.com/docs/html/hollywood/SetDefaultEncoding_.html).",
    "inputsDocs": "* `id` — identifier of path to which the text should be added\n* `t$` — text to add to path\n* `table` — optional: table containing further arguments (see above)",
    "example": "```\nEnableLayers\nSetFillStyle(#FILLCOLOR)\nSetFormStyle(#ANTIALIAS)\nSetFont(\"Arial\", 100, {Engine = #FONTENGINE_INBUILT})\nStartPath(1)\nMoveTo(1, 0, 0)\nAddTextToPath(1, \"Hello World\")\nDrawPath(1, #CENTER, #CENTER + 100, #BLUE, {AnchorX = 0.5,\n    AnchorY = 0.5, Rotate = 45})\n```\n\nThe code above creates a vector path containing the text \"Hello World\". The path is then drawn using rotation by 45 degrees.",
    "platforms": ["All"],
    "insertText": "AddTextToPath(${1:id}, ${2:t$})"
  },
  {
    "name": "AppendPath",
    "helpId": "AppendPath",
    "shortDescription": "append path to another path",
    "version": "5.0",
    "synopsis": "AppendPath(id, src)",
    "functionDocs": "This function appends the path specified in `src` to the end of the path specified in `id`. All path data is copied so that you can free the `src` path after the append operation.",
    "inputsDocs": "* `id` — identifier of path that shall be modified\n* `src` — identifier of path that shall be appended",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "AppendPath(${1:id}, ${2:src})"
  },
  {
    "name": "ClearPath",
    "helpId": "ClearPath",
    "shortDescription": "remove all vertices from a path",
    "version": "5.0",
    "synopsis": "ClearPath(id)",
    "functionDocs": "This command will clear the specified path completely. All path vertices and all sub-paths will be removed. However, the path itself will not be freed. Thus, you can start adding vertices to it again after this call. If you want to free a path completely, use [FreePath()](https://www.hollywood-mal.com/docs/html/hollywood/FreePath_.html) instead.",
    "inputsDocs": "* `id` — identifier of path to clear",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "ClearPath(${1:id})"
  },
  {
    "name": "ClosePath",
    "helpId": "ClosePath",
    "shortDescription": "close current sub-path",
    "version": "5.0",
    "synopsis": "ClosePath(id)",
    "functionDocs": "This command will close the current sub-path in the path specified by `id`. The sub-path is closed by adding a line to the starting point of the current sub-path. If you want to add new vertices after calling `ClosePath()`, you have to call [StartSubPath()](https://www.hollywood-mal.com/docs/html/hollywood/StartSubPath_.html) and [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) to create a new sub-path.",
    "inputsDocs": "* `id` — identifier of path to close",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "ClosePath(${1:id})"
  },
  {
    "name": "CopyPath",
    "helpId": "CopyPath",
    "shortDescription": "clone a path",
    "version": "5.0",
    "synopsis": "[id] = CopyPath(src, dst)",
    "functionDocs": "This command clones the path specified in `src` and creates a new path under the identifier `dst` that is an exact copy of the path specified in argument 1. The `dst` argument can either be an identifier that should be used for the new path or it can be [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html), in which case `CopyPath()` will automatically select an identifier and return it to you.",
    "inputsDocs": "* `src` — identifier of path to clone\n* `dst` — identifier for the new path or [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) for [auto id selection](https://www.hollywood-mal.com/docs/html/hollywood/AutoIDSelect_.html)",
    "resultsDocs": "* `id` — optional: identifier of the new path; this will only be returned when you pass [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) as argument 2 (see above)",
    "platforms": ["All"],
    "insertText": "CopyPath(${1:src}, ${2:dst})"
  },
  {
    "name": "CurveTo",
    "helpId": "CurveTo",
    "shortDescription": "add curve to path",
    "version": "5.0",
    "synopsis": "CurveTo(id, x1, y1, x2, y2, x3, y3)",
    "functionDocs": "This command will add a cubic B�zier spline curve to the path. The curve will run from the current point to the position specified in `x3`,`y3`. The control points of the spline curve have to specified in arguments `x1`,`y1` and `x2`,`y2`. When `CurveTo()` returns, the path's current point will be at `x3`,`y3`.",
    "inputsDocs": "* `id` — identifier of path to add curve to\n* `x1` — x coordinate of control point `#1`\n* `y1` — y coordinate of control point `#1`\n* `x2` — x coordinate of control point `#2`\n* `y2` — y coordinate of control point `#2`\n* `x3` — x coordinate of curve destination point\n* `y3` — y coordinate of curve destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "CurveTo(${1:id}, ${2:x1}, ${3:y1}, ${4:x2}, ${5:y2}, ${6:x3}, ${7:y3})"
  },
  {
    "name": "DrawPath",
    "helpId": "DrawPath",
    "shortDescription": "draw vector path",
    "version": "5.0",
    "synopsis": "DrawPath(id, x, y[[, color], table])",
    "functionDocs": "This draws the vector path specified in `id` to the position specified in `x` and `y` using the color that is passed in argument 4. The vector path will be drawn using the form style specified using [SetFormStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetFormStyle_.html) and it will be filled according to the configuration selected using [SetFillStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetFillStyle_.html) and [SetFillRule()](https://www.hollywood-mal.com/docs/html/hollywood/SetFillRule_.html). If you are drawing the vector path in outline mode (i.e. fill style is set to `#FILLNONE`), then `DrawPath()` will also take the settings of [SetLineJoin()](https://www.hollywood-mal.com/docs/html/hollywood/SetLineJoin_.html), [SetLineCap()](https://www.hollywood-mal.com/docs/html/hollywood/SetLineCap_.html), and [SetDash()](https://www.hollywood-mal.com/docs/html/hollywood/SetDash_.html) into account. `Color` can either be an [RGB value](https://www.hollywood-mal.com/docs/html/hollywood/RGBInfo_.html) or an [ARGB value](https://www.hollywood-mal.com/docs/html/hollywood/ARGBInfo_.html) for alpha-blended drawing.\n\nThe optional table argument can be used to specify one or more of the [standard tags for all drawing commands](https://www.hollywood-mal.com/docs/html/hollywood/StandardDrawTags_.html). See [Standard drawing tags](https://www.hollywood-mal.com/docs/html/hollywood/StandardDrawTags_.html).\n\nIf layers are enabled, this command will add a new layer of the type `#VECTORPATH` to the layer stack.\n\nNote that `DrawPath()` only allows you to use a single color for per path. If you want to use multi-colored paths, you can use the [PathToBrush()](https://www.hollywood-mal.com/docs/html/hollywood/PathToBrush_.html) function to combine multiple paths inside a single vector brush object. See [PathToBrush](https://www.hollywood-mal.com/docs/html/hollywood/PathToBrush_.html).\n\nNote that when drawing to a palette-based target and the palette mode is set to `#PALETTEMODE_PEN`, this function will draw using the pen set via [SetDrawPen()](https://www.hollywood-mal.com/docs/html/hollywood/SetDrawPen_.html) instead of the color passed to the function.",
    "inputsDocs": "* `id` — identifier of path object to draw\n* `x` — destination x offset\n* `y` — destination y offset\n* `color` — optional: [RGB](https://www.hollywood-mal.com/docs/html/hollywood/RGBInfo_.html) or [ARGB](https://www.hollywood-mal.com/docs/html/hollywood/ARGBInfo_.html) color (defaults to `#BLACK`) color is optional because it is not required when you draw to a mask or alpha channel\n* `table` — optional: table containing further arguments; can be any from the ones listed above and from the [standard tags](https://www.hollywood-mal.com/docs/html/hollywood/StandardDrawTags_.html)",
    "example": "```\nSetFillStyle(#FILLNONE)\nSetFormStyle(#ANTIALIAS)\nx=25.6 y=128.0\nx1=102.4 y1=230.4\nx2=153.6 y2=25.6\nx3=230.4 y3=128.0\n\nStartPath(1)\nMoveTo(1, x, y)\nCurveTo(1, x1, y1, x2, y2, x3, y3)\nSetLineWidth(10)\nDrawPath(1, 0, 0, #BLACK)\n\nClearPath(1)\nMoveTo(1, x, y)\nLineTo(1, x1, y1)\nMoveTo(1, x2, y2)\nLineTo(1, x3, y3)\nSetLineWidth(6)\nDrawPath(1, 0, 0, ARGB(128, #RED))\n```\n\nThe code above draws a curve and two lines that illustrate the control points of the curve.\n\n---\n\n```\nEnableLayers\nSetFillStyle(#FILLCOLOR)\nSetFormStyle(#ANTIALIAS)\nStartPath(1)\nAddBoxToPath(1, 0, 0, 100, 100)\nAddBoxToPath(1, 150, 0, 100, 100)\nAddBoxToPath(1, 0, 150, 100, 100)\nAddBoxToPath(1, 150, 150, 100, 100)\nDrawPath(1, #CENTER, #CENTER, #RED, {Border = True, bordersize = 5})\n```\n\nThe code above draws a vector path that looks a little bit like the flag of Switzerland.",
    "platforms": ["All"],
    "insertText": "DrawPath(${1:id}, ${2:x}, ${3:y})"
  },
  {
    "name": "ForcePathUse",
    "helpId": "ForcePathUse",
    "shortDescription": "always use path-based drawing",
    "version": "5.0",
    "synopsis": "ForcePathUse(enable)",
    "functionDocs": "This command can be used to redirect all drawing commands of Hollywood's standard graphics primitives library to the new vector-path based draw library. This is especially useful for round shapes like circles, arcs, and ellipses, because the standard drawing library is line-based, which means that round shapes will never look perfectly round because their round shape must be approximated through lines. The vector-path based drawing library on the other hand can draw perfectly round shapes which will look better than the line-based approach of the standard drawing library.\n\nTo enable the path-based drawing for the standard functions, pass `True` to `ForcePathUse()`. Then, the following standard functions will be patched to make use of the new vector-path based drawing: [Arc()](https://www.hollywood-mal.com/docs/html/hollywood/Arc_.html), [Box()](https://www.hollywood-mal.com/docs/html/hollywood/Box_.html), [Circle()](https://www.hollywood-mal.com/docs/html/hollywood/Circle_.html), [Ellipse()](https://www.hollywood-mal.com/docs/html/hollywood/Ellipse_.html), and [Polygon()](https://www.hollywood-mal.com/docs/html/hollywood/Polygon_.html).\n\nKeep in mind, though, that if you enable path-based drawing for the standard library, you will always get layers of type `#VECTORPATH` if layers are enabled. The standard draw library on the other hand would add layers of type `#ARC`, `#BOX`, `#CIRCLE`, `#ELLIPSE`, and `#POLYGON` respectively. Normally, this does not make much of a difference, but it can be an issue if you try to change the attributes of a layer using [SetLayerStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetLayerStyle_.html), because layers of type `#VECTORPATH` do not have the same functionality than the other layer types. See [SetLayerStyle](https://www.hollywood-mal.com/docs/html/hollywood/SetLayerStyle_.html).\n\nPlease also note that in Hollywood versions earlier than 6.0 your script will require a vectorgraphics plugin if you pass `True` here. Starting with Hollywood 6.0 there is an inbuilt vectorgraphics engine as well.\n\nFinally, it should be mentioned that vector-path based drawing is of course slower than the polygon based drawing of the standard drawing library. On modern systems, however, it does not make a great difference.",
    "inputsDocs": "* `enable` — flag that specifies whether or not to enable the path-based drawing for standard functions (`True` means enable, `False` means disable)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "ForcePathUse(${1:enable})"
  },
  {
    "name": "FreePath",
    "helpId": "FreePath",
    "shortDescription": "free path object",
    "version": "5.0",
    "synopsis": "FreePath(id)",
    "functionDocs": "This command will free the path specified in `id`. After calling this command the specified path will not be available any more. If you only want to remove all vertices and sub-paths from a path, you should use [ClearPath()](https://www.hollywood-mal.com/docs/html/hollywood/ClearPath_.html) instead.",
    "inputsDocs": "* `id` — identifier of path to free",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "FreePath(${1:id})"
  },
  {
    "name": "GetCurrentPoint",
    "helpId": "GetCurrentPoint",
    "shortDescription": "get current point of path",
    "version": "5.0",
    "synopsis": "x, y = GetCurrentPoint(id)",
    "functionDocs": "This command returns the current point of the path specified in `id`.",
    "inputsDocs": "* `id` — identifier of path to query",
    "resultsDocs": "* `x` — x position of current point\n* `y` — y position of current point",
    "platforms": ["All"],
    "insertText": "GetCurrentPoint(${1:id})"
  },
  {
    "name": "GetDash",
    "helpId": "GetDash",
    "shortDescription": "get current line dashing style",
    "version": "7.1",
    "synopsis": "offset[, t] = GetDash()",
    "functionDocs": "This function returns the current line dashing style set using [SetDash()](https://www.hollywood-mal.com/docs/html/hollywood/SetDash_.html). The first return value will be set to the offset at which the dash pattern starts and the second return value is a table containing the lengths of the individual on/off sections. See [SetDash](https://www.hollywood-mal.com/docs/html/hollywood/SetDash_.html).\n\nIf no dash pattern is currently active, -1 is returned in `offset` and there is no second return value.",
    "inputsDocs": "none",
    "resultsDocs": "* `offset` — offset at which to start the dash pattern or -1 for no dash pattern\n* `t` — optional: table containing on/off sections (only if `offset` is not -1)",
    "platforms": ["All"],
    "insertText": "GetDash()"
  },
  {
    "name": "GetFillRule",
    "helpId": "GetFillRule",
    "shortDescription": "get current fill rule for overlapping paths",
    "version": "7.1",
    "synopsis": "rule = GetFillRule()",
    "functionDocs": "This function returns the current fill rule set using [SetFillRule()](https://www.hollywood-mal.com/docs/html/hollywood/SetFillRule_.html). This will be either `#FILLRULEWINDING` or `#FILLRULEEVENODD`. See [SetFillRule](https://www.hollywood-mal.com/docs/html/hollywood/SetFillRule_.html).",
    "inputsDocs": "none",
    "resultsDocs": "* `rule` — current fill rule",
    "platforms": ["All"],
    "insertText": "GetFillRule()"
  },
  {
    "name": "GetLineCap",
    "helpId": "GetLineCap",
    "shortDescription": "get current line cap style",
    "version": "7.1",
    "synopsis": "style = GetLineCap()",
    "functionDocs": "This function returns the current line cap style set using [SetLineCap()](https://www.hollywood-mal.com/docs/html/hollywood/SetLineCap_.html). This will be either `#CAPBUTT`, `#CAPROUND`, or `#CAPSQUARE`. See [SetLineCap](https://www.hollywood-mal.com/docs/html/hollywood/SetLineCap_.html).",
    "inputsDocs": "none",
    "resultsDocs": "* `style` — current line cap style",
    "platforms": ["All"],
    "insertText": "GetLineCap()"
  },
  {
    "name": "GetLineJoin",
    "helpId": "GetLineJoin",
    "shortDescription": "get current line join style",
    "version": "7.1",
    "synopsis": "style = GetLineJoin()",
    "functionDocs": "This function returns the current line join style set using [SetLineJoin()](https://www.hollywood-mal.com/docs/html/hollywood/SetLineJoin_.html). This will be either `#JOINMITER`, `#JOINROUND`, or `#JOINBEVEL`. See [SetLineJoin](https://www.hollywood-mal.com/docs/html/hollywood/SetLineJoin_.html).",
    "inputsDocs": "none",
    "resultsDocs": "* `style` — current line join style",
    "platforms": ["All"],
    "insertText": "GetLineJoin()"
  },
  {
    "name": "GetMiterLimit",
    "helpId": "GetMiterLimit",
    "shortDescription": "get current miter limit",
    "version": "7.1",
    "synopsis": "limit = GetMiterLimit()",
    "functionDocs": "This function returns the current miter limit set using [SetMiterLimit()](https://www.hollywood-mal.com/docs/html/hollywood/SetMiterLimit_.html). See [SetMiterLimit](https://www.hollywood-mal.com/docs/html/hollywood/SetMiterLimit_.html).",
    "inputsDocs": "none",
    "resultsDocs": "* `limit` — current miter limit",
    "platforms": ["All"],
    "insertText": "GetMiterLimit()"
  },
  {
    "name": "GetPathExtents",
    "helpId": "GetPathExtents",
    "shortDescription": "calculate extents of path",
    "version": "5.0",
    "synopsis": "x1, y1, x2, y2 = GetPathExtents(id)",
    "functionDocs": "This command calculates the extents of the path specified in `id`. The current fill style and form style is taken into account by this function. The extents are returned as a bounding rectangle. Note that all return values specify absolute point positions. To get the width and height of the path from these positions you have to subtract `x1` from `x2` and `y1` from `y2`.",
    "inputsDocs": "* `id` — identifier of path to query",
    "resultsDocs": "* `x1` — start x position\n* `y1` — start y position\n* `x2` — end x position\n* `y2` — end y position",
    "platforms": ["All"],
    "insertText": "GetPathExtents(${1:id})"
  },
  {
    "name": "IsPathEmpty",
    "helpId": "IsPathEmpty",
    "shortDescription": "check if path is empty",
    "version": "5.0",
    "synopsis": "ret = IsPathEmpty(id)",
    "functionDocs": "This function can be used to check whether or not the specified path object is empty or not. If it is empty, this function returns `True`, otherwise `False`.",
    "inputsDocs": "* `id` — identifier of path to query",
    "resultsDocs": "* `ret` — `True` if path is empty, else `False`",
    "platforms": ["All"],
    "insertText": "IsPathEmpty(${1:id})"
  },
  {
    "name": "LineTo",
    "helpId": "LineTo",
    "shortDescription": "add line to path",
    "version": "5.0",
    "synopsis": "LineTo(id, x, y)",
    "functionDocs": "This command will add a line from the path's current point to the point specified by `x`,`y`. When `LineTo()` returns, the path's current point will be at `x`,`y`.",
    "inputsDocs": "* `id` — identifier of path to add line to\n* `x` — x coordinate of destination point\n* `y` — y coordinate of destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "LineTo(${1:id}, ${2:x}, ${3:y})"
  },
  {
    "name": "MoveTo",
    "helpId": "MoveTo",
    "shortDescription": "set current point and begin sub-path",
    "version": "5.0",
    "synopsis": "MoveTo(id, x, y)",
    "functionDocs": "This command can be used to begin a new sub-path at the specified point. When `MoveTo()` returns, the path's current point will be at `x`,`y`. For most cases this is the preferable way to start a new sub-path. The command [StartSubPath()](https://www.hollywood-mal.com/docs/html/hollywood/StartSubPath_.html) is only recommended for the rare case when you want a sub-path without a current point.",
    "inputsDocs": "* `id` — identifier of path\n* `x` — x coordinate of destination point\n* `y` — y coordinate of destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "MoveTo(${1:id}, ${2:x}, ${3:y})"
  },
  {
    "name": "NormalizePath",
    "helpId": "NormalizePath",
    "shortDescription": "move path to origin",
    "version": "5.0",
    "synopsis": "NormalizePath(id)",
    "functionDocs": "This function can be used to normalize a path. Normalizing means that all blank spaces at the left and top sides of the path are cut off, so that the path is moved to the origin position at the top left corner.",
    "inputsDocs": "* `id` — identifier of path to normalize",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "NormalizePath(${1:id})"
  },
  {
    "name": "PathItems",
    "helpId": "PathItems",
    "shortDescription": "traverse individual path items",
    "version": "7.0",
    "synopsis": "f = PathItems(id)",
    "functionDocs": "This function can be used together with the generic [For](https://www.hollywood-mal.com/docs/html/hollywood/PrgFor_.html) statement to traverse all items in a path. It returns an iterator function which will return a table that contains information about the next item in the path. Once all path items have been returned, the iterator function will return [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) to break the generic [For](https://www.hollywood-mal.com/docs/html/hollywood/PrgFor_.html) statement.\n\nSee [Generic For statement](https://www.hollywood-mal.com/docs/html/hollywood/PrgFor_.html).\n\nThe table returned by `PathItems()` will contain a `Type` field which contains a string that describes the item type. All further table fields depend on the item type as returned in `Type`. The following types are currently supported:\n\n- `NewSubPath`: This item starts a new sub-path. The current point will be undefined at this time. There are no additional arguments.\n\n- `ClosePath`: Closes the current path by drawing a line from the current point to the first point in the sub-path. There are no additional arguments.\n\n- `MoveTo`: This command begins a new sub-path. The sub-path's current point will be set to the specified position. The `MoveTo` table contains the following three additional arguments:\n\n  - `Rel`: This is a boolean value that indicates whether the coordinates are relative or absolute values. If this is `True`, the coordinates have to be interpreted as relative to the current point.\n\n  - `X`: The x coordinate of the new position.\n\n  - `Y`: The y coordinate of the new position.\n\n- `LineTo`: This draws a line from the current point to the specified position. Additionally, it will change the current point to the line's end point when it is done. The `LineTo` table contains the following three additional arguments:\n\n  - `Rel`: This is a boolean value that indicates whether the coordinates are relative or absolute values. If this is `True`, the coordinates have to be interpreted as relative to the current point.\n\n  - `X`: The x coordinate of the new position.\n\n  - `Y`: The y coordinate of the new position.\n\n  If there is no current point, `LineTo` will behave as if it was `MoveTo`, i.e. it will simply set the current point to the specified vertex.\n\n- `CurveTo`: This command draws a B�zier curve that runs from the current point to the position passed in the final two coordinates. The other four coordinates are the control points for the curve. Additionally, it will change the current point to the curve's end point when it is done. The `CurveTo` table contains the following seven additional arguments:\n\n  - `Rel`: This is a boolean value that indicates whether the coordinates are relative or absolute values. If this is `True`, the coordinates have to be interpreted as relative to the current point.\n\n  - `X1`: The x coordinate of the first control point.\n\n  - `Y1`: The y coordinate of the first control point.\n\n  - `X2`: The x coordinate of the second control point.\n\n  - `Y2`: The y coordinate of the second control point.\n\n  - `X3`: The x coordinate of the end point.\n\n  - `Y3`: The y coordinate of the end point.\n\n  If there is no current point, `CurveTo` will use the point passed in (x1,y1) as the current point.\n\n- `Arc`: This command will draw an elliptical arc. `Arc` will open a new subpath for the new arc only in case there is no currently active subpath. If there is already a subpath, `Arc` will connect its starting vertex with the current vertex of the subpath. `Arc` will not close the subpath when it has finished adding its vertices. `Arc` will not connect the start and end angles of the arc with its center point automatically. This has to be requested explicitly by issuing separate `MoveTo` and `LineTo` commands before and after `Arc`. The `Arc` table contains the following additional arguments:\n\n  - `XC`: The x center point of the arc.\n  - `YC`: The y center point of the arc.\n  - `RA`: Arc's radius on the x axis.\n  - `RB`: Arc's radius on the y axis.\n  - `Start`: Start angle in degrees.\n  - `End`: End angle in degrees.\n  - `Clockwise`: Whether or not the angles should be connected in clockwise direction.\n\n  When `Arc` is done, it will set the current point to the position of the end angle.\n\n- `Box`: This command will draw a rectangle. `Box` will first open a new subpath, then add the rectangle's vertices to it and close the subpath when it is finished. Optionally, the rectangle can have rounded corners. The `Box` table contains the following additional arguments:\n\n  - `X`: X position of the rectangle.\n  - `Y`: Y position of the rectangle.\n  - `Width`: Rectangle width.\n  - `Height`: Rectangle height.\n  - `Round`: This is a table which contains four integer values in the range from 0 to 100 specifying the degree of rounding for the four corners of the rectangle. A value of 0 means no rounding, 100 means full rounding.\n\n- `Text`: This command will draw vector text relative to the current point. The individual characters are added as closed subpaths. The `Text` table contains the following additional arguments:\n\n  - `Size`: Desired font size.\n\n  - `Text`: The string to draw.\n\n  When `Text` is done, it will set the current point to where the next character would be displayed.",
    "inputsDocs": "* `id` — identifier of path to traverse",
    "resultsDocs": "* `f` — iterator function for generic for loop",
    "example": "```\nFor t In PathItems(1) Do DebugPrint(t.type)\n```\n\nThe code above iterates over all items in path 1 and prints the type of each item to the debug device.",
    "platforms": ["All"],
    "insertText": "PathItems(${1:id})"
  },
  {
    "name": "PathToBrush",
    "helpId": "PathToBrush",
    "shortDescription": "convert path(s) to vector brush",
    "version": "7.0",
    "synopsis": "[id] = PathToBrush(id, table)",
    "functionDocs": "This function can be used to convert one or more path(s) into a vector brush. `PathToBrush()` will create a new brush with the identifier specified in `id` or if you pass [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) in the `id` argument, `PathToBrush()` will automatically choose an identifier for the new brush and return it to you.\n\nConverting paths into vector brushes has the advantage that you can assign different colors to the individual paths combined inside a single vector brush, allowing you to easily manage multi-colored paths inside just a single brush object. Furthermore, the paths combined inside the vector brush can also use different drawing styles.\n\nYou have to pass a table in the `table` argument that contains a number of subtables specifying information about the individual paths to be embedded inside the vector brush. The paths are drawn into the vector brush in exactly the same order as they appear in that table.\n\nNote that each path to be embedded inside the vector brush will be normalized first. Thus, by default all paths will be drawn in the top-left corner of the vector brush. You can change this behaviour by specifying the `X` and `Y` arguments in the individual subtables for each path to be added to the vector brush (see below).\n\nThe following subtable fields can be specified:\n\n- `ID`: This must be set to the identifier of the path object to be embedded inside the vector brush that shall be created. This must always be provided. Note that `PathToBrush()` will make a copy of this path so subsequent modifications of the path won't affect the new vector brush in any way. You may also free this path after adding it to the vector brush.\n\n- `X`: The x position where this path should be drawn inside the vector brush. This position must be relative to the left corner of the vector brush. Note that `PathToBrush()` will internally normalize the path before adding it to the vector brush so you will usually have to use this field to position the path correctly inside the vector brush. Defaults to 0.\n\n- `Y`: The y position where this path should be drawn inside the vector brush. This position must be relative to the top corner of the vector brush. Note that `PathToBrush()` will internally normalize the path before adding it to the vector brush so you will usually have to use this field to position the path correctly inside the vector brush. Defaults to 0.\n\n- `Color`: The path will be drawn in this [ARGB color](https://www.hollywood-mal.com/docs/html/hollywood/ARGBInfo_.html). This color can also contain a transparency setting. Defaults to `#BLACK`.\n\nNote that the form and fill styles to be used by the individual paths are the ones that were active at the time the path was created using [StartPath()](https://www.hollywood-mal.com/docs/html/hollywood/StartPath_.html). This is different to the way form and fill styles work when drawing paths using [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html). [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) will use the form and fill styles that are active when [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) is called whereas `PathToBrush()` will use the form and fill styles that were active when [StartPath()](https://www.hollywood-mal.com/docs/html/hollywood/StartPath_.html) was called on the individual paths. This allows you to use different form and fill styles for the individual paths to be embedded inside the vector brush.",
    "inputsDocs": "* `id` — id for the new brush or [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) for [auto id selection](https://www.hollywood-mal.com/docs/html/hollywood/AutoIDSelect_.html)\n* `table` — table containing paths to combine into the vector brush (see above)",
    "resultsDocs": "* `id` — optional: identifier of the brush; will only be returned when you pass [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) as argument 1 (see above)",
    "example": "```\nPathToBrush(1, {{ID=1, Color=#RED}, {ID=2, Color=#BLUE, X=100}})\n```\n\nThe code above combines paths 1 and 2 inside a new vector brush which will use the identifier 1. Path 1 is drawn in red and path 2 is drawn in blue. Additionally, path 2 is drawn at x position 100 whereas path 1 is drawn at 0,0.",
    "platforms": ["All"],
    "insertText": "PathToBrush(${1:id}, ${2:table})"
  },
  {
    "name": "RelCurveTo",
    "helpId": "RelCurveTo",
    "shortDescription": "add curve to path",
    "version": "5.0",
    "synopsis": "RelCurveTo(id, dx1, dy1, dx2, dy2, dx3, dy3)",
    "functionDocs": "This command does the same as [CurveTo()](https://www.hollywood-mal.com/docs/html/hollywood/CurveTo_.html) except that the coordinates are delta values instead of absolute positions. The delta coordinates are all interpreted as relative offsets to the path's current point.",
    "inputsDocs": "* `id` — identifier of path to add curve to\n* `dx1` — delta x coordinate of control point `#1`\n* `dy1` — delta y coordinate of control point `#1`\n* `dx2` — delta x coordinate of control point `#2`\n* `dy2` — delta y coordinate of control point `#2`\n* `dx3` — delta x coordinate of curve destination point\n* `dy3` — delta y coordinate of curve destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "RelCurveTo(${1:id}, ${2:dx1}, ${3:dy1}, ${4:dx2}, ${5:dy2}, ${6:dx3}, ${7:dy3})"
  },
  {
    "name": "RelLineTo",
    "helpId": "RelLineTo",
    "shortDescription": "add relative line to path",
    "version": "5.0",
    "synopsis": "RelLineTo(id, dx, dy)",
    "functionDocs": "This command does the same as [LineTo()](https://www.hollywood-mal.com/docs/html/hollywood/LineTo_.html) except that the coordinates are delta values instead of absolute positions. The delta coordinates are all interpreted as relative offsets to the path's current point.",
    "inputsDocs": "* `id` — identifier of path to add line to\n* `dx` — delta x coordinate of destination point\n* `dy` — delta y coordinate of destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "RelLineTo(${1:id}, ${2:dx}, ${3:dy})"
  },
  {
    "name": "RelMoveTo",
    "helpId": "RelMoveTo",
    "shortDescription": "set relative current point and begin sub-path",
    "version": "5.0",
    "synopsis": "RelMoveTo(id, dx, dy)",
    "functionDocs": "This command does the same as [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) except that the coordinates are delta values instead of absolute positions. The delta coordinates are all interpreted as relative offsets to the path's current point.",
    "inputsDocs": "* `id` — identifier of path\n* `dx` — delta x coordinate of destination point\n* `dy` — delta y coordinate of destination point",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "RelMoveTo(${1:id}, ${2:dx}, ${3:dy})"
  },
  {
    "name": "SetDash",
    "helpId": "SetDash",
    "shortDescription": "set line dashing style",
    "version": "5.0",
    "synopsis": "SetDash(offset, on1, off1, ...)",
    "functionDocs": "This function can be used to define a dash pattern for paths drawn by [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html). A dash pattern consists of an unlimited number of on and off line sections that start at the offset specified in argument 1. Starting with argument 2, you have to pass the length of the line section that shall be visible (\"on-section\") followed by the length of the line section that shall be invisible (\"off-section\"), and repeat this pattern as many times as you like. When drawing an outline path, [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) will then apply this dash pattern to all lines it is drawing. When the dash pattern does not cover the whole line length, it will be repeated over and over again.\n\nTo remove the dash pattern, call this function with the second argument set to -1.\n\nPlease note that the line dash style is only used when drawing vector outlines. It is obviously not used when filling vector paths.\n\nAlso note that the inbuilt vectorgraphics renderer introduced in Hollywood 6.0 currently does not support line dashing. Use a fully featured vectorgraphics plugin instead if you need line dashing. See [Vectorgraphics plugin note](https://www.hollywood-mal.com/docs/html/hollywood/VectorGfxNote_.html).",
    "inputsDocs": "* `offset` — offset at which to start the dash pattern\n* `on1` — length of on-section 1\n* `off1` — length of off-section 1\n* `...` — optional: define as many on-/off-sections as you like",
    "example": "```\nSetFillStyle(#FILLNONE)\nSetFormStyle(#ANTIALIAS)\nSetLineWidth(10)\nSetDash(0, 10, 10, 20, 20, 30, 30, 40, 40)\nStartPath(1)\nMoveTo(1, 0, 0)\nLineTo(1, 640, 480)\nDrawPath(1, 0, 0, #RED)\n```\n\nThe code above draws a line using a dash pattern that first has four different on/off sections: The first on/off section is 10 units, the second 20 units, the third 30 units, and the fourth 40 units.",
    "platforms": ["All"],
    "insertText": "SetDash(${1:offset}, ${2:on1}, ${3:off1})"
  },
  {
    "name": "SetFillRule",
    "helpId": "SetFillRule",
    "shortDescription": "set fill rule for overlapping paths",
    "version": "5.0",
    "synopsis": "SetFillRule(rule)",
    "functionDocs": "This function can be used to define how [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) should fill paths that overlap each other. Currently, the following fill rules are supported:\n\n- `#FILLRULEWINDING`: Fill all overlapping paths only if they are not winding. This is the default setting.\n\n- `#FILLRULEEVENODD`: Fill overlapping paths if the total number of intersections is odd.",
    "inputsDocs": "* `rule` — desired fill rule (see above for possible settings)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetFillRule(${1:rule})"
  },
  {
    "name": "SetLineCap",
    "helpId": "SetLineCap",
    "shortDescription": "define current line cap style",
    "version": "5.0",
    "synopsis": "SetLineCap(style)",
    "functionDocs": "This function can be used to define how [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) should draw the endings of lines that are not connected to another vertices. Currently, the following cap styles are supported:\n\n- `#CAPBUTT`: Do not draw any special line ending. Just stop drawing where the line ends. This is the default mode.\n\n- `#CAPROUND`: Draw round line endings.\n\n- `#CAPSQUARE`: Draw squared line endings.\n\nPlease note that the line cap style is only used when drawing vector outlines. It is obviously not used when filling vector paths.",
    "inputsDocs": "* `style` — desired line cap style (see above for possible settings)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetLineCap(${1:style})"
  },
  {
    "name": "SetLineJoin",
    "helpId": "SetLineJoin",
    "shortDescription": "define current line join style",
    "version": "5.0",
    "synopsis": "SetLineJoin(style)",
    "functionDocs": "This function can be used to define how [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) should connect the lines when drawing a vector path. Currently, the following join styles are supported:\n\n- `#JOINMITER`: Use miter join (a sharp angled corner). This is the default join mode. The miter limit can be set using the [SetMiterLimit()](https://www.hollywood-mal.com/docs/html/hollywood/SetMiterLimit_.html) function. See [SetMiterLimit](https://www.hollywood-mal.com/docs/html/hollywood/SetMiterLimit_.html).\n\n- `#JOINROUND`: Join lines by drawing their ends as circles. This gives a thick pen impression.\n\n- `#JOINBEVEL`: Join lines by cutting off the line ends at the half of the line width.\n\nPlease note that the line join style is only used when drawing vector outlines. It is obviously not used when filling vector paths.",
    "inputsDocs": "* `style` — desired line join style (see above for possible settings)",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetLineJoin(${1:style})"
  },
  {
    "name": "SetMiterLimit",
    "helpId": "SetMiterLimit",
    "shortDescription": "set miter limit",
    "version": "7.1",
    "synopsis": "SetMiterLimit(limit)",
    "functionDocs": "This function sets the miter limit to the value specified by `limit`. This can be a fractional value. The miter limit is used when the join style is set to `#JOINMITER` to determine when to join lines with a bevel and when to join them using a miter. Note that `#JOINMITER` is also the default line join style.\n\nWhen drawing line ends, the length of the miter is divided by the line width and if the result of this division is greater than the miter limit set using this function, lines are joined using a bevel.\n\nHollywood's default miter limit is 10.",
    "inputsDocs": "* `limit` — desired miter limit",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetMiterLimit(${1:limit})"
  },
  {
    "name": "SetVectorEngine",
    "helpId": "SetVectorEngine",
    "shortDescription": "choose vectorgraphics renderer",
    "version": "6.0",
    "synopsis": "SetVectorEngine(engine$)",
    "functionDocs": "This command can be used to select the plugin that should be used to draw vector-based shapes. You simply have to pass the name of the plugin to this function. To use the inbuilt vectorgraphics renderer, pass `default`.\n\nNote that it is perfectly allowed to use several vectorgraphics renderers inside a single script. It is even possible to use several vectorgraphics renderers inside a single BGPic. For example, the first layer could use the default vectorgraphics renderer while the second layer uses a plugin-based vectorgraphics renderer. This is all supported. You can even change the renderer used by single layers by calling [SetLayerStyle()](https://www.hollywood-mal.com/docs/html/hollywood/SetLayerStyle_.html) and setting the `VectorEngine` tag.",
    "inputsDocs": "* `engine$` — name of the plugin that should be used to draw vectorgraphics",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "SetVectorEngine(${1:engine$})"
  },
  {
    "name": "StartPath",
    "helpId": "StartPath",
    "shortDescription": "begin a new path",
    "version": "5.0",
    "synopsis": "[id] = StartPath(id)",
    "functionDocs": "This command can be used to create a new path object that will be made available under the identifier `id`. Alternatively, you can pass [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) as `id`. In that case, `StartPath()` will automatically select an identifier and return it to you.\n\nOnce the new path is created, you should first define a current point for the path by calling the [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) command. After that, you can start adding vertices to the path.\n\nA vector path is a root object for an infinite number of sub-paths. You can think of a vector path as a collection of an infinite number of separate polygons. Each sub-path within a vector path can be regarded as a separate polygon. Keep in mind, though, that when you draw a vector path using [DrawPath()](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html) you can only specify a global color that will be used by all sub-paths within the vector path. Thus, it is not possible to use different colors within a single path object. If you need to use another color, you need to create a new path object first.",
    "inputsDocs": "* `id` — identifier for the new path or [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) for [auto id selection](https://www.hollywood-mal.com/docs/html/hollywood/AutoIDSelect_.html)",
    "resultsDocs": "* `id` — optional: identifier of the new path; this will only be returned when you pass [Nil](https://www.hollywood-mal.com/docs/html/hollywood/PrgTypeNil_.html) as argument 1 (see above)",
    "example": "See [DrawPath](https://www.hollywood-mal.com/docs/html/hollywood/DrawPath_.html)",
    "platforms": ["All"],
    "insertText": "StartPath(${1:id})"
  },
  {
    "name": "StartSubPath",
    "helpId": "StartSubPath",
    "shortDescription": "begin a new sub-path",
    "version": "5.0",
    "synopsis": "StartSubPath(id)",
    "functionDocs": "This command can be used to begin a new sub-path within the path object specified in `id`. The new sub-path will not get a current point so that most of the time you should better call [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) to start a new sub-path. `StartSubPath()` is only preferable for rare cases in which a current point is not desired; for example, when adding a circle/ellipse/arc to a path a current point can be annoying because it would be connected to the circle/ellipse/arc then. For most cases, however, you should use [MoveTo()](https://www.hollywood-mal.com/docs/html/hollywood/MoveTo_.html) instead of `StartSubPath()`.",
    "inputsDocs": "* `id` — identifier of the path to use",
    "resultsDocs": "none",
    "platforms": ["All"],
    "insertText": "StartSubPath(${1:id})"
  },
  {
    "name": "TranslatePath",
    "helpId": "TranslatePath",
    "shortDescription": "offset a path",
    "version": "5.0",
    "synopsis": "TranslatePath(id, dx, dy)",
    "functionDocs": "This function can be used to translate a path. Translating means that each vertex in the path is shifted by the specified delta offset. A positive delta offset shifts to the right (or bottom) and a negative delta offset shifts to the left (or top). An offset of 0 does not do anything.",
    "inputsDocs": "* `id` — identifier of path to normalize\n* `dx` — amount of delta shift on the x axis\n* `dy` — amount of delta shift on the y axis",
    "example": "```\nTranslatePath(1, -100, 150)\n```\n\nThe code above shifts path number one 100 pixels to the left and 150 pixels to the bottom.",
    "platforms": ["All"],
    "insertText": "TranslatePath(${1:id}, ${2:dx}, ${3:dy})"
  }
]
